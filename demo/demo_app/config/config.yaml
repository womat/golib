# =============================================================================
# app configuration
# =============================================================================

# logLevel defines the minimum log level.
# Messages with at least this level are logged.
# Allowed values: debug | info | warn | error
logLevel: info

# logDestination defines where logs are written to.
# Supported values: stdout | stderr | /path/to/logfile
logDestination: stdout

# environment: dev | prod
env: dev


# =============================================================================
# Webserver configuration (HTTPS)
# =============================================================================
webserver:
  # Host address the HTTPS server listens on (0.0.0.0 = all interfaces)
  listenHost: 0.0.0.0

  # Port the HTTPS server listens on
  listenPort: 8443

  # Global API key for protected endpoints.
  #
  # Secrets belong in the environment, not in this file - a value may reference
  # a variable as ${NAME}, which is the recommended form for a deployment:
  #
  #   apiKey: ${DEMO_APP_API_KEY}
  #
  # An undefined variable stops the service instead of producing an empty
  # secret. A bare $NAME is not expanded, so a literal value like pa$$w0rd stays
  # what it is. With env: prod this key must not stay "changeme!".
  apiKey: changeme!

  # Secret and ID for JWT authentication; both together, or neither.
  # The secret belongs in the environment as well.
  # jwtSecret: ${DEMO_APP_JWT_SECRET}
  # jwtID: demo_app

  # TLS private key file
  keyFile: /opt/demo_app/etc/key.pem

  # TLS certificate file
  certFile: /opt/demo_app/etc/cert.pem

  # Blocked IP addresses or networks (empty = none blocked)
  # Examples: 192.168.0.1, 192.168.0.0/16, 10.0.0.0/8
  blockedIPs: []
  #  - 192.168.0.1
  #  - 192.168.0.0/16

  # Allowed IP addresses or networks (empty = all allowed)
  # Note: ::1 is the IPv6 loopback address
  # Examples: 127.0.0.1, ::1, 192.168.0.0/16
  allowedIPs: []
  #  - 127.0.0.1
  #  - ::1
  #  - 192.168.0.0/16

